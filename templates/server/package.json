{
  "devDependencies": {
    "purescript": "^0.15.15",
    "spago": "^0.93.44"
  },
  "scripts": {
    "build": "spago build",
    "serve": "deno run --allow-net serve.ts",
    "test": "deno run --allow-all test.ts"
  }
}
